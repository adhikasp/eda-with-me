{
  "name": "my-evidence-project",
  "version": "0.0.1",
  "scripts": {
    "dev": "svelte-kit dev --host 0.0.0.0 --port 3000",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview --host 0.0.0.0 --port 3000"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "type": "module",
  "dependencies": {
    "@evidence-dev/evidence": "^0.1.17"
  }
}
